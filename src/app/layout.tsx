export const metadata = {
    title: "Fuel Tracker",
    description: "Fleet fuel tracking application",
};

export default function RootLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <html lang="en">
            <body>{children}</body>
        </html>
    );
}
